// models/RSchedule.js
const mongoose = require('mongoose');

const railSSchema = new mongoose.Schema({
 
});

// Create a model for the 'railS' collection
const railSModel = mongoose.model('railS', railSSchema);

module.exports = railSModel;
