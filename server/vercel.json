{
  "version": 2,
  "builds": [
    { "src": "server/index.js", "use": "@now/node-server" }
  ],
  "routes": [
    { "src": "/api/(.*)", "dest": "server/Routes/$1" },
    { "src": "/(.*)", "dest": "server/index.js" }
  ]
}
