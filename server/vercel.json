{
  "version": 2,
  "builds": [{ "src": "server", "use": "@now/node-server" }],
  "routes": [
    { "handle": "filesystem" },
    { "src": "/api/(.*)", "dest": "server/Routes/$1" },
    { "src": "/(.*)", "dest": "server/index.js" }
  ],
}
